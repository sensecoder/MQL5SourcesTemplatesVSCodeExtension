export class Insertor {
   /**
    * Вставляет шаблон с задными параметрами в открытый editor соответствующего файла.
    * @param options Параметры шаблона
    * @param fileName Имя файла, к которому будет применятся шаблон
    */
   constructor(options: any, fileName: string) {
      
   }

   public applyTemplate(): boolean {

      return false;
   }
}